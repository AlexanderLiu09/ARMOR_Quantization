defaults:
  - ARMOR
  - override ./@compression_config.naive_compression_config: prune_quantize
  - _self_

init_config:
  _target_: "src.ARMOR_Quantize.QuantizedARMOR_Linear"
  verbose: True
compression_config:
  block_diagonal_config:
      block_size: 128
  training_config:
    log_freq: 1000
    n_sparse_core_updates_per_iter: 0